<div class=" my-2">
    <nav class=" ">
            <ul class="nav nav-pills nav-fill bg-light">
                <li class="nav-item">
                    <a class="nav-link fs-6 fw-semibold active applyTo" href="/enrolled/application/<%= enrolledId %>/applyTo">Apply To Programs</a>
                </li>
                <li class="nav-item">
                    <% if(applications.length !== 0){ %> 
                    <a class="nav-link fs-6 fw-semibold applied <%= (applications)?"":"disabled" %>" href="/enrolled/application/<%= enrolledId %>/applied/<%= applications[0]._id %>">Applied Programs</a>
                    <% } else { %>
                    <a class="nav-link fs-6 fw-semibold applied disabled" href="#">Applied Programs</a>
                    <% } %>
                </li>
            </ul>
    </nav>
</div>